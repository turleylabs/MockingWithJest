function forEach(items, callback) {
    for (let index = 0; index < items.length; index++) {
        callback(items[index]);
    }
}

xtest('jest mocks', () => {
});

xtest('more jest mocks', () => {
});

